# 安全资源分享网 - 环境变量配置模板
# 复制此文件为 .env.local 并填入实际值

# ===========================================
# 数据库配置 (必填)
# ===========================================
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ===========================================
# 认证配置 (必填 - 安全关键)
# ===========================================
# JWT密钥 - 必须至少32个字符，使用强随机值
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-at-least-32-characters-long"

# 应用URL
NEXTAUTH_URL="https://yourdomain.com"

# ===========================================
# 管理员配置 (必填)
# ===========================================
# 管理员邮箱列表，逗号分隔
ADMIN_EMAILS="admin@yourdomain.com,superadmin@yourdomain.com"

# ===========================================
# Ping++ 支付配置 (可选 - 启用支付功能需要)
# ===========================================
PING_APP_ID=""
PING_API_KEY=""
PING_WEBHOOK_KEY=""
PING_API_URL="https://api.pingxx.com"
PING_WEBHOOK_URL="https://yourdomain.com/api/payments/callback"

# ===========================================
# 应用配置
# ===========================================
NEXT_PUBLIC_APP_NAME="安全资源分享网"
NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# ===========================================
# 安全注意事项
# ===========================================
# 1. 绝不要将此文件提交到版本控制
# 2. 在生产环境使用环境变量管理服务（如 Vercel 环境变量）
# 3. 定期轮换 NEXTAUTH_SECRET 和 API 密钥
# 4. 确保数据库连接使用 SSL (sslmode=require)
# 5. 设置强密码策略，密钥长度至少32字符
